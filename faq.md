# Часто задаваемые вопросы

## Общие вопросы

### Что такое Razd?

Razd (от русского "раздуплиться" — проснуться и взять себя в руки) — это современный CLI инструмент на Rust, который автоматизирует полный цикл настройки проекта. Он оркестрирует работу git, mise и taskfile, превращая многошаговый процесс клонирования, установки инструментов и настройки зависимостей в одну команду `razd up`.

**Ключевая идея:** Вместо запоминания и выполнения десятка команд для каждого нового проекта, вы выполняете одну — и проект готов к работе.

### Чем Razd отличается от других инструментов настройки?

Razd уникален по нескольким причинам:

1. **Интеграция вместо замены** — использует проверенные инструменты экосистемы (mise, taskfile) вместо изобретения собственных
2. **Нулевая конфигурация** — работает с существующими `.mise.toml` и `Taskfile.yml` без изменений
3. **Истинная кроссплатформенность** — одинаковое поведение на Windows, macOS и Linux
4. **Фокус на DX** — понятные сообщения об ошибках, интуитивный API
5. **Автоматическая синхронизация** — согласованность между `Razdfile.yml` и `mise.toml`
6. **Автоопределение проектов** — создает правильную конфигурацию для Node.js, Python, Rust, Go и других
7. **Одна команда для всего** — от клонирования до запуска dev сервера

### Какая текущая версия Razd?

Последняя стабильная версия — **v0.4.2** (ноябрь 2024).

Проверьте вашу версию:
```bash
razd --version
```

### Нужно ли модифицировать существующие проекты для использования Razd?

**Нет!** Razd работает с существующими конфигурациями:

- ✅ Если есть `.mise.toml` и `Taskfile.yml` — работает сразу
- ✅ Если есть только `.mise.toml` — будет использовать его
- ✅ Если есть только `Taskfile.yml` — будет использовать его
- ✅ Если есть `Razdfile.yml` — объединяет обе конфигурации

**Опционально:** Создайте `Razdfile.yml` для централизованной конфигурации, но это не обязательно.

### В чем разница между Razdfile.yml, mise.toml и Taskfile.yml?

- **`Razdfile.yml`** — унифицированная конфигурация Razd, объединяющая управление инструментами (mise) и задачи (taskfile) в одном файле
- **`.mise.toml`** (или `mise.toml`) — конфигурация mise для управления версиями инструментов
- **`Taskfile.yml`** — конфигурация taskfile для определения задач автоматизации

**Razd автоматически синхронизирует** `Razdfile.yml` ↔ `.mise.toml` для согласованности.

## Установка и настройка

### Какие платформы поддерживает Razd?

Razd работает на всех основных платформах:

| Платформа | Архитектура | Статус |
|-----------|-------------|--------|
| **Windows** | x64 | ✅ Полная поддержка |
| **macOS** | Intel (x86_64) | ✅ Полная поддержка |
| **macOS** | Apple Silicon (ARM64) | ✅ Полная поддержка |
| **Linux** | x64 | ✅ Полная поддержка |

**Терминалы:**
- Windows: PowerShell, Command Prompt, Git Bash, WSL
- macOS/Linux: bash, zsh, fish, и другие POSIX-совместимые

### Нужно ли устанавливать mise и task отдельно?

**mise** — да, обязательно. Это ключевая зависимость.

**task** — нет, опционально. Razd автоматически установит task через mise при первой необходимости.

**Установка mise:**
```bash
# Unix/Linux/macOS
curl https://mise.run | sh

# macOS (Homebrew)
brew install mise

# Windows
winget install jdx.mise
```

### Можно ли использовать Razd без mise?

В настоящее время **нет**. mise — обязательная зависимость, так как Razd использует его для:
- Управления версиями инструментов разработки
- Обеспечения изоляции проектов
- Установки более 500 инструментов экосистемы

Однако, если ваш проект не требует специальных инструментов, можете использовать только задачи:

```yaml
version: '3'
tasks:
  default:
    cmds:
      - echo "Hello from Razd!"
```

### Как обновить Razd до последней версии?

**Через mise (рекомендуется):**
```bash
# До последней версии
mise use -g razd@latest

# До конкретной версии
mise use -g razd@0.4.2

# Проверьте обновление
razd --version
```

**Для ручных установок:**
Скачайте новый исполняемый файл с [GitHub Releases](https://github.com/razd-cli/razd/releases/latest) и замените старый.

## Вопросы по использованию

### Что происходит, когда я запускаю `razd up`?

`razd up` выполняет полный цикл настройки:

```
razd up [URL]
    ↓
┌─────────────────────────────────────┐
│ 1. Клонирование (если указан URL)  │
│    git clone [URL]                  │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 2. Чтение конфигурации              │
│    Razdfile.yml или                 │
│    mise.toml + Taskfile.yml         │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 3. Синхронизация конфигурации       │
│    Razdfile.yml ↔ mise.toml         │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 4. Установка инструментов           │
│    mise install (Node.js, Python...)│
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 5. Выполнение default задачи        │
│    - Установка зависимостей         │
│    - Настройка БД                   │
│    - Первичная сборка               │
│    - Запуск dev сервера             │
└─────────────────────────────────────┘
    ↓
    ✅ Проект готов!
```

### Какие команды доступны в Razd?

| Команда | Описание | Пример |
|---------|----------|--------|
| `razd up [URL]` | Клонировать и настроить проект (или настроить локальный) | `razd up https://github.com/user/repo` |
| `razd up --init` | Создать Razdfile.yml с автоопределением типа проекта | `razd up --init` |
| `razd install` | Установить инструменты из Razdfile.yml | `razd install` |
| `razd setup` | Установить зависимости проекта | `razd setup` |
| `razd dev` | Запустить dev сервер | `razd dev` |
| `razd build` | Собрать проект | `razd build` |
| `razd run <task>` | Выполнить кастомную задачу | `razd run test` |
| `razd --version` | Показать версию | `razd --version` |
| `razd --help` | Показать справку | `razd --help` |

### Как передать аргументы в задачу?

Используйте флаг `--args`:

```bash
# Передать аргументы в задачу
razd run deploy --args "--env=production --verbose"

# В Razdfile.yml получите их через {{.CLI_ARGS}}
tasks:
  deploy:
    cmds:
      - ./scripts/deploy.sh {{.CLI_ARGS}}
```

### Можно ли использовать Razd с приватными репозиториями?

**Да!** Razd использует ваши существующие git credentials:

```bash
# SSH (рекомендуется для приватных репо)
razd up git@github.com:company/private-repo.git

# HTTPS (потребуется ввод credentials)
razd up https://github.com/company/private-repo.git
```

Убедитесь, что:
- SSH ключи настроены (`ssh -T git@github.com`)
- Или HTTPS credentials сохранены (`git config credential.helper`)

### Как отключить автоматическую синхронизацию?

**Для одной команды:**
```bash
razd install --no-sync
razd up --no-sync
```

**Через переменную окружения:**
```bash
# Unix/Linux/macOS
export RAZD_NO_SYNC=1
razd up

# Windows (PowerShell)
$env:RAZD_NO_SYNC=1
razd up
```

**Когда отключать:**
- При отладке конфликтов конфигурации
- Когда хотите вручную контролировать mise.toml
- В CI/CD где конфигурация уже синхронизирована

## Конфигурация

### Как создать Razdfile.yml с нуля?

**Автоматически (рекомендуется):**
```bash
cd my-project
razd up --init
```

Razd определит тип проекта и создаст подходящую конфигурацию.

**Вручную:**
```yaml
version: '3'

mise:
  tools:
    node: "22"
    python: "3.11"

tasks:
  default:
    desc: Настроить и запустить проект
    cmds:
      - task: install
      - task: dev
  
  install:
    cmds:
      - mise install
      - npm install
  
  dev:
    cmds:
      - npm run dev
```

### Какие инструменты поддерживает mise?

**Более 500 инструментов!** Включая:

**Языки программирования:**
- JavaScript/TypeScript: `node`, `deno`, `bun`
- Python: `python`, `poetry`, `pipenv`
- Ruby: `ruby`, `bundler`
- Go: `go`
- Rust: `rust`, `cargo`
- Java: `java`, `maven`, `gradle`
- PHP: `php`, `composer`
- И многие другие

**Инструменты разработки:**
- Базы данных: `postgres`, `mysql`, `redis`, `mongodb`
- Облачные CLI: `aws`, `gcloud`, `azure-cli`, `kubectl`
- Утилиты: `docker`, `terraform`, `ansible`, `gh`

**Полный список:** [mise plugins](https://mise.jdx.dev/plugins.html)

### Как настроить инструмент с переменными окружения?

Используйте расширенный синтаксис в `Razdfile.yml`:

```yaml
mise:
  tools:
    python:
      version: "3.11"
      postinstall: |
        pip install --upgrade pip
        pip install poetry
      os: ["linux", "macos"]
      install_env:
        PYTHON_CONFIGURE_OPTS: "--enable-optimizations"
        PYTHON_CFLAGS: "-O3"
    
    go:
      version: "1.21"
      postinstall: "go install golang.org/x/tools/cmd/goimports@latest"
      install_env:
        CGO_ENABLED: "1"
        GOARCH: "amd64"
```

### Как создать композитные задачи?

Используйте `task:` для вызова других задач:

```yaml
tasks:
  # Атомарные задачи
  clean:
    cmds: [rm -rf dist/]
  
  test:
    cmds: [npm test]
  
  build:
    cmds: [npm run build]
  
  # Композитная задача
  ci:
    desc: Полный CI pipeline
    cmds:
      - task: clean
      - task: test
      - task: build
  
  # С зависимостями
  deploy:
    deps: [build]  # Выполнить build перед deploy
    cmds:
      - ./scripts/deploy.sh
```

### Можно ли использовать переменные в задачах?

**Да!** Taskfile поддерживает переменные:

```yaml
tasks:
  build:
    cmds:
      - echo "Building for {{.ENV | default \"dev\"}}"
      - npm run build -- --mode={{.ENV}}
  
  deploy:
    cmds:
      - task: build
        vars:
          ENV: production
      - ./deploy.sh {{.TARGET}} {{.VERSION}}
```

**Использование:**
```bash
# Через CLI_ARGS
razd run deploy --args "staging v1.0.0"

# Или через env переменные в задаче
```

## Устранение неполадок

### Ошибка: "mise not found"

**Решение:**
```bash
# Установите mise
curl https://mise.run | sh

# Обновите PATH
source ~/.bashrc  # или ~/.zshrc для zsh

# Проверьте
mise --version
```

### Ошибка: "No Razdfile.yml found"

**Решение:**
```bash
# Вариант 1: Создать автоматически
razd up --init

# Вариант 2: Создать минимальный вручную
cat > Razdfile.yml << 'EOF'
version: '3'
mise:
  tools:
    node: "22"
tasks:
  default:
    cmds:
      - echo "Setup complete!"
EOF
```

### Конфликт между Razdfile.yml и mise.toml

**Причина:** Оба файла содержат разные версии инструментов.

**Решение:**
```bash
# Вариант 1: Позволить Razd синхронизировать автоматически
razd up
# Razd спросит, какой файл приоритетный

# Вариант 2: Отключить синхронизацию
razd up --no-sync

# Вариант 3: Вручную решить конфликт
# Выберите один источник истины (Razdfile.yml рекомендуется)
```

### Задача не найдена

**Проблема:**
```bash
razd run my-task
# Error: Task 'my-task' not found
```

**Решение:**
```bash
# Проверьте доступные задачи
razd run  # Без аргументов выведет список

# Или откройте файл
cat Razdfile.yml | grep -A 5 "tasks:"

# Убедитесь, что задача определена:
# tasks:
#   my-task:  # ← Должна быть
#     cmds: [...]
```

### Инструмент не установился

**Проблема:**
```bash
razd install
# Failed to install node@22
```

**Решение:**
```bash
# Проверьте доступность плагина
mise plugins list

# Установите плагин вручную, если нужно
mise plugin install node

# Проверьте доступные версии
mise ls-remote node

# Попробуйте снова
razd install
```

### Windows: "Execution policy error"

**Проблема:** PowerShell блокирует выполнение скриптов.

**Решение:**
```powershell
# Проверьте текущую политику
Get-ExecutionPolicy

# Установите на текущий сеанс
Set-ExecutionPolicy -Scope Process -ExecutionPolicy Bypass

# Или для текущего пользователя (рекомендуется)
Set-ExecutionPolicy -Scope CurrentUser -ExecutionPolicy RemoteSigned
```

### macOS: "Developer cannot be verified"

**Проблема:** macOS блокирует неподписанные бинарники.

**Решение:**
```bash
# Разрешите запуск
xattr -d com.apple.quarantine /usr/local/bin/razd

# Или через System Preferences → Security & Privacy → "Allow anyway"
```

## Интеграция и продвинутое использование

### Как использовать Razd в CI/CD?

**GitHub Actions:**
```yaml
name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Install mise
        run: curl https://mise.run | sh
      
      - name: Install Razd
        run: |
          $HOME/.local/bin/mise plugin install razd https://github.com/razd-cli/vfox-plugin-razd
          $HOME/.local/bin/mise use -g razd@0.4.2
      
      - name: Setup and test
        run: |
          razd up
          razd run test
          razd run build
```

**GitLab CI:**
```yaml
test:
  image: ubuntu:latest
  before_script:
    - apt-get update && apt-get install -y curl
    - curl https://mise.run | sh
    - source $HOME/.local/share/mise/shims
    - mise plugin install razd https://github.com/razd-cli/vfox-plugin-razd
    - mise use -g razd@0.4.2
  script:
    - razd up
    - razd run test
    - razd run build
```

### Можно ли использовать Razd как библиотеку Rust?

**Да!** Razd предоставляет Rust API:

```rust
use razd::config::RazdfileConfig;
use razd::integrations::{mise, taskfile};

#[tokio::main]
async fn main() -> razd::Result<()> {
    // Загрузить конфигурацию
    let config = RazdfileConfig::load()?;
    
    // Установить инструменты
    mise::install_tools(".").await?;
    
    // Выполнить задачу
    taskfile::execute_workflow_task("dev", &workflow, ".").await?;
    
    Ok(())
}
```

См. [Library API](https://docs.rs/razd) для полной документации.

### Как работать с монорепозиториями?

**Вариант 1: Razdfile.yml в корне**
```yaml
# ./Razdfile.yml
version: '3'
mise:
  tools:
    node: "22"
tasks:
  setup-all:
    cmds:
      - cd frontend && npm install
      - cd backend && npm install
```

**Вариант 2: Отдельные Razdfile.yml в каждом проекте**
```bash
# Настройка конкретного проекта
cd frontend
razd up

cd ../backend
razd up
```

### Можно ли расширить Razd плагинами?

В настоящее время Razd не имеет системы плагинов, но вы можете:

1. **Использовать кастомные задачи в Razdfile.yml**
2. **Создавать wrapper скрипты**
3. **Вносить вклад в основной проект** на [GitHub](https://github.com/razd-cli/razd)

## Производительность и оптимизация

### Сколько времени занимает настройка проекта?

**Традиционный подход:** 5-15 минут (ручная работа)  
**С Razd:** 30-120 секунд (автоматически)

**Факторы, влияющие на время:**
- Размер репозитория
- Количество инструментов для установки
- Скорость интернета
- Объем зависимостей проекта

### Как ускорить повторные установки?

mise кэширует установленные инструменты глобально:

```bash
# Первая установка: ~30 сек (скачивание)
razd up https://github.com/user/nodejs-project

# Повторная установка в другом проекте: ~5 сек (кэш)
razd up https://github.com/user/another-nodejs-project
```

### Потребляет ли Razd много места на диске?

**Сам Razd:** ~10-15 MB (один исполняемый файл)

**mise cache:** Зависит от установленных инструментов:
- Node.js: ~50-100 MB на версию
- Python: ~100-200 MB на версию
- Go: ~150-300 MB на версию

**Очистка кэша:**
```bash
# Удалить неиспользуемые версии
mise prune

# Удалить все установленные инструменты
mise uninstall --all
```

## Сообщество и поддержка

### Где получить помощь?

1. 📖 **Документация:** [razd-cli.github.io/razd](https://razd-cli.github.io/razd)
2. 💬 **GitHub Discussions:** [Задать вопрос](https://github.com/razd-cli/razd/discussions)
3. 🐛 **GitHub Issues:** [Сообщить о баге](https://github.com/razd-cli/razd/issues)
4. 💡 **Примеры:** [Реальные проекты](https://github.com/razd-cli/razd/tree/main/examples)

### Как внести вклад в Razd?

Мы приветствуем вклады! 🎉

1. 🍴 **Fork репозитория**
2. 🔧 **Создайте feature branch:** `git checkout -b my-feature`
3. ✅ **Добавьте тесты** для новой функциональности
4. 📝 **Обновите документацию**
5. 🚀 **Создайте Pull Request**

См. [CONTRIBUTING.md](https://github.com/razd-cli/razd/blob/main/CONTRIBUTING.md) для деталей.

### Какие проекты используют Razd?

Razd используется в:
- Open source проектах для упрощения онбординга контрибьюторов
- Компаниях для стандартизации dev окружений
- Образовательных туториалах и мастер-классах
- CI/CD pipeline для воспроизводимых сборок

Поделитесь вашим кейсом в [Show and Tell](https://github.com/razd-cli/razd/discussions/categories/show-and-tell)!

### Планы развития Razd?

- 🔌 Система плагинов
- 🌐 Поддержка больше CI/CD платформ
- 📊 Аналитика производительности сетапа
- 🎨 Интерактивный TUI
- 🐳 Docker интеграция
- 📦 Встроенный менеджер зависимостей

Следите за [Roadmap](https://github.com/razd-cli/razd/projects) и [предлагайте идеи](https://github.com/razd-cli/razd/discussions/categories/ideas)!

---

**Не нашли ответ?** [Задайте вопрос в Discussions](https://github.com/razd-cli/razd/discussions/new?category=q-a) 💬

`razd up` выполняет эти шаги:
1. **Клонирование**: Если указан URL, клонирует репозиторий
2. **Обнаружение**: Ищет `.mise.toml` и `Taskfile.yml`
3. **Установка**: Запускает `mise install`, если найдена конфигурация
4. **Настройка**: Запускает `task setup`, если найден Taskfile
5. **Отчёт**: Показывает, что было выполнено

### Можно ли использовать `razd up` на существующем проекте?

Да! Запуск `razd up` в существующей директории проекта:
- Пропустит шаг клонирования
- Установит инструменты через mise (если настроено)
- Запустит задачи настройки (если доступно)

### Что если в моём проекте нет задачи `setup`?

Razd пропустит шаг настройки и сообщит вам об этом. Вы можете создать задачу `setup` в вашем `Taskfile.yml`, чтобы определить, что должно происходить при инициализации проекта.

### Могу ли я настроить, какая задача запускается при настройке?

В настоящее время Razd запускает задачу `setup` по умолчанию. Настройка пользовательских задач планируется для будущих релизов. Пока вы можете:
- Использовать `razd install` + `razd task your-custom-task`
- Создать задачу `setup`, которая вызывает ваши пользовательские задачи

### Как посмотреть доступные задачи?

```bash
razd task --list
# или
task --list
```

## Конфигурация и кастомизация

### Что такое Razdfile.yml?

Razdfile.yml — это опциональный конфигурационный файл, который позволяет настроить поведение Razd. Вы можете создать его с помощью:
```bash
razd init
```

### Можно ли настроить Razd глобально?

Да, вы можете создать глобальный конфигурационный файл в `~/.config/razd/config.yml` или использовать переменные окружения, такие как `RAZD_VERBOSE=1`.

### Как настроить Razd для моей команды?

1. Убедитесь, что в вашем проекте есть `.mise.toml` с необходимыми инструментами
2. Создайте `Taskfile.yml` с задачей `setup`
3. Члены команды просто запускают `razd up` для начала
4. Рассмотрите добавление `Razdfile.yml` для специфичной для проекта конфигурации

### Можно ли использовать Razd с монорепозиториями?

Да! Razd отлично работает с монорепозиториями. Вы можете:
- Иметь настройку верхнего уровня для общих инструментов
- Использовать индивидуальные Taskfile в каждом пакете
- Запускать `razd up` из любой директории с конфигурацией

## Устранение неполадок

### "Command not found: razd"

Это означает, что Razd не в вашем PATH. Решения:
1. Переустановите, используя [руководство по установке](/installation/)
2. Проверьте, что исполняемый файл исполняемый (`chmod +x razd` на Unix)
3. Убедитесь, что ваш PATH включает директорию установки

### "mise command not found"

Razd требует mise для управления инструментами:
```bash
# Установите mise
curl https://mise.jdx.dev/install.sh | sh
# или
brew install mise
```

### Настройка не удаётся с "Task 'setup' not found"

Ваш `Taskfile.yml` не имеет задачи `setup`. Вы можете:
1. Добавить задачу `setup` в ваш Taskfile
2. Использовать `razd install` для установки только инструментов
3. Запускать задачи вручную с `razd task task-name`

### Инструменты не устанавливаются

Проверьте, что:
1. У вас есть файл `.mise.toml` или `.tool-versions`
2. Файл правильно отформатирован
3. У вас есть сетевой доступ для скачивания инструментов
4. mise работает: `mise doctor`

### Git clone не удаётся

Распространённые причины:
1. **Репозиторий не существует**: Проверьте URL
2. **Нет доступа**: Проверьте ваши учётные данные для приватных репозиториев
3. **Проблемы с сетью**: Проверьте ваше интернет-соединение
4. **SSH ключи**: Для SSH URL убедитесь, что ваши ключи настроены

### Задачи не запускаются

Шаги отладки:
1. Запустите с подробным выводом: `razd --verbose task task-name`
2. Проверьте определение задачи в `Taskfile.yml`
3. Проверьте, что все зависимости установлены
4. Попробуйте запустить задачу напрямую: `task task-name`

## Настройка проекта

### Как создать новый проект с Razd?

1. Создайте новую директорию: `mkdir my-project && cd my-project`
2. Инициализируйте конфигурацию: `razd init`
3. Отредактируйте сгенерированные файлы под ваши нужды
4. Добавьте файлы вашего проекта
5. Протестируйте с `razd up`

### Что должно быть в моей задаче `setup`?

Распространённые задачи настройки включают:
```yaml
setup:
  deps: [install-deps, setup-env, setup-db]
  cmds:
    - echo "Настройка проекта завершена!"

install-deps:
  cmds:
    - npm install # или pip install и т.д.

setup-env:
  cmds:
    - cp .env.example .env

setup-db:
  cmds:
    - task db:create
    - task db:migrate
```

### Как обрабатывать чувствительную конфигурацию?

Лучшие практики:
1. Используйте файлы `.env.example` с значениями-заполнителями
2. Копируйте в `.env` в вашей задаче setup
3. Никогда не коммитьте чувствительные значения в git
4. Используйте mise для управления API ключами, когда возможно

## Вопросы интеграции

### Можно ли использовать Razd в CI/CD?

Абсолютно! Razd идеален для CI/CD:
```yaml
# Пример GitHub Actions
- name: Setup project
  run: razd up
```

Преимущества:
- Согласованная настройка окружения
- Быстрее, чем ручные шаги
- Легко поддерживать

### Работает ли Razd с Docker?

Да! Вы можете:
- Использовать Razd для настройки проектов, использующих Docker
- Запускать Razd внутри Docker контейнеров
- Включать Docker инструменты в ваш `.mise.toml`

### Можно ли использовать Razd с существующими инструментами сборки?

Razd дополняет существующие инструменты:
- **npm/yarn**: Отлично работает с проектами Node.js
- **cargo**: Идеально для проектов Rust
- **pip/poetry**: Превосходно для проектов Python
- **make**: Может использоваться вместе с или вместо task

### Как Razd работает с IDE?

Razd настраивает окружение вашего проекта, которое затем могут использовать IDE:
- VS Code: Автоматически обнаружит версии инструментов
- IntelliJ: Может использовать инструменты, управляемые mise
- Vim/Neovim: Работает с любой конфигурацией LSP

## Производительность и лучшие практики

### Насколько быстр Razd?

Razd разработан для скорости:
- Написан на Rust для производительности
- Параллельная установка инструментов, где возможно
- Умное кэширование сокращает повторные операции
- Типичная настройка: от 30 секунд до 2 минут в зависимости от размера проекта

### Лучшие практики для адаптации команды?

1. **Начните просто**: Начните с базовых `.mise.toml` и `Taskfile.yml`
2. **Документируйте настройку**: Включите инструкции Razd в ваш README
3. **Предоставьте примеры**: Покажите распространённые команды и процессы
4. **Регулярно тестируйте**: Проверяйте, что настройка работает в чистых окружениях
5. **Будьте последовательны**: Используйте похожие паттерны в проектах

### Как оптимизировать производительность настройки?

Советы для более быстрой настройки:
1. **Используйте кэширование mise**: Инструменты кэшируются после первой установки
2. **Параллельные задачи**: Используйте зависимости задач умно
3. **Минимальные установки**: Включайте только необходимые инструменты в `.mise.toml`
4. **Локальные зеркала**: Используйте локальные зеркала пакетов, когда доступно

## Получение помощи

### Где можно получить поддержку?

- **Документация**: Этот сайт охватывает большинство распространённых сценариев
- **GitHub Issues**: [Сообщить об ошибках или запросить функции](https://github.com/razd-cli/razd/issues)
- **GitHub Discussions**: [Вопросы и ответы сообщества](https://github.com/razd-cli/razd/discussions)

### Как сообщить об ошибке?

Пожалуйста, включите:
1. Вашу операционную систему и версию
2. Версию Razd (`razd --version`)
3. Полное сообщение об ошибке
4. Шаги для воспроизведения
5. Ожидаемое vs фактическое поведение

### Как я могу внести вклад?

Мы приветствуем вклады:
- **Документация**: Улучшите эту документацию
- **Сообщения об ошибках**: Помогите нам найти и исправить проблемы
- **Запросы функций**: Предложите улучшения
- **Код**: Отправьте pull request
- **Примеры**: Поделитесь настройками ваших проектов

Проверьте [репозиторий GitHub](https://github.com/razd-cli/razd) для более подробной информации о вкладе.
