# Что такое Razd?

Razd (от русского "раздуплиться" — проснуться и взять себя в руки) — это современный CLI инструмент на Rust, который радикально упрощает инициализацию и настройку проектов, оркестрируя работу mise и taskfile для создания бесшовного опыта разработки.

## Проблема

Настройка нового проекта разработки обычно включает множество ручных шагов, которые занимают время и подвержены ошибкам:

```bash
# Традиционный подход — множество команд
git clone https://github.com/company/awesome-project.git
cd awesome-project
mise install          # Установка инструментов разработки
npm install           # Или pip install, bundle install и т.д.
cp .env.example .env  # Настройка конфигурации
npm run build         # Первоначальная сборка
task db:migrate       # Настройка базы данных
# ... ещё больше команд настройки
```

Этот процесс:
- **Занимает много времени**: 5-10+ минут ручной работы на каждый проект
- **Подвержен ошибкам**: Легко пропустить шаги или выполнить их в неправильном порядке
- **Непостоянный**: У каждого проекта свои уникальные процедуры настройки
- **Раздражающий для новичков**: Особенно для новых членов команды или контрибьюторов
- **Плохо документирован**: Инструкции часто устаревают или неполны

## Решение

Razd автоматизирует весь этот процесс одной командой:

```bash
# Способ Razd — одна команда, все готово
razd up https://github.com/company/awesome-project.git
```

Вот и всё! Razd автоматически:
1. ✅ Клонирует репозиторий
2. ✅ Устанавливает все необходимые инструменты разработки через mise
3. ✅ Настраивает зависимости проекта
4. ✅ Запускает начальную сборку
5. ✅ Выполняет все кастомные задачи настройки
6. ✅ Готов к разработке!

## Как работает Razd

Razd интегрирует три проверенных инструмента экосистемы для создания мощного рабочего процесса:

### 1. **git** — Управление репозиториями
- Клонирует репозитории с любого Git хостинга (GitHub, GitLab, Bitbucket и др.)
- Поддерживает все Git протоколы (HTTPS, SSH, Git)
- Работает с публичными и приватными репозиториями
- Автоматически определяет имя проекта из URL

### 2. **mise** — Управление версиями инструментов
- Автоматически устанавливает необходимые инструменты разработки (Node.js, Python, Ruby, Go, Rust и более 500 других)
- Обеспечивает единообразие версий инструментов для всех членов команды
- Изолирует версии инструментов для каждого проекта
- Поддерживает сложные конфигурации с переменными окружения и постустановочными скриптами

### 3. **taskfile** — Автоматизация задач
- Выполняет задачи настройки проекта (установка зависимостей, миграции БД и т.д.)
- Определяет рабочие процессы через декларативный YAML
- Поддерживает зависимости между задачами и параллельное выполнение
- Предоставляет унифицированный интерфейс для проектов на разных языках

### Рабочий процесс Razd

```
razd up [URL]
    ↓
1. Клонирует репозиторий (если указан URL)
    ↓
2. Читает Razdfile.yml
    ↓
3. Синхронизирует с mise.toml (если нужно)
    ↓
4. Устанавливает инструменты через mise
    ↓
5. Выполняет default задачу (или указанную)
    ↓
6. ✓ Проект готов к работе!
```

## Ключевые возможности

### 🚀 Инициализация проекта одной командой

Клонируйте, настройте и запустите проект одной командой:

```bash
# Клонирование и настройка удаленного репозитория
razd up https://github.com/user/project.git

# С кастомным именем директории
razd up https://github.com/user/project.git --name my-custom-name

# Настройка существующего локального проекта
cd my-project
razd up
```

### 🔧 Автоматическое определение типа проекта

Инициализируйте Razdfile.yml с автоматическим определением технологического стека:

```bash
cd my-nodejs-app
razd up --init

# Razd автоматически определит:
# - Node.js (если есть package.json)
# - Python (если есть requirements.txt, pyproject.toml, setup.py)
# - Rust (если есть Cargo.toml)
# - Go (если есть go.mod)
# - Docker (если есть Dockerfile)
# - И создаст соответствующий Razdfile.yml
```

### 📦 Управление инструментами разработки

Установите все инструменты, определенные в конфигурации:

```bash
# Установка всех инструментов из Razdfile.yml
razd install

# Без автоматической синхронизации
razd install --no-sync
```

### ⚙️ Установка зависимостей проекта

Автоматизируйте установку зависимостей:

```bash
# Запустить задачу setup (npm install, pip install и т.д.)
razd setup
```

### 🏃 Запуск рабочих процессов разработки

```bash
# Запустить dev сервер
razd dev

# Собрать проект
razd build

# Выполнить кастомную задачу
razd run test
razd run deploy --args "--env=production"
```

## Конфигурация: Razdfile.yml

Razdfile.yml — это центральный файл конфигурации, объединяющий управление инструментами и автоматизацию задач.

### Базовая структура

```yaml
version: '3'

# Конфигурация mise для управления инструментами
mise:
  tools:
    node: "22"        # Простая версия
    python: "3.11"
    task: "latest"
  
  # Кастомные плагины mise (опционально)
  plugins:
    node: "https://github.com/asdf-vm/asdf-nodejs.git"

# Определения задач (формат Taskfile v3)
tasks:
  default:
    desc: Настроить и запустить проект
    cmds:
      - task: install
      - task: setup
      - task: dev
  
  install:
    desc: Установить инструменты разработки
    cmds:
      - mise install
      - echo "Инструменты установлены"
  
  setup:
    desc: Установить зависимости проекта
    cmds:
      - npm install
      - npm run build
  
  dev:
    desc: Запустить dev сервер
    cmds:
      - npm run dev
  
  build:
    desc: Собрать проект для продакшена
    cmds:
      - npm run build
      - npm run test
```

### Расширенная конфигурация инструментов

Поддержка сложных конфигураций с переменными окружения:

```yaml
mise:
  tools:
    # Простая версия
    node: "22"
    
    # Расширенная конфигурация с опциями
    python:
      version: "3.11"
      postinstall: |
        pip install --upgrade pip setuptools wheel
        pip install poetry
      os: ["linux", "macos"]
      install_env:
        PYTHON_CONFIGURE_OPTS: "--enable-optimizations"
    
    go:
      version: "1.21"
      postinstall: |
        go install golang.org/x/tools/cmd/goimports@latest
        go install github.com/golangci/golangci-lint/cmd/golangci-lint@latest
      install_env:
        CGO_ENABLED: "1"
        GOARCH: "amd64"
```

### Композиция задач с переменными

Создавайте переиспользуемые задачи с передачей переменных:

```yaml
tasks:
  # Переиспользуемые задачи
  clean:
    desc: Очистить артефакты сборки
    cmds:
      - rm -rf dist/ build/
  
  test:
    desc: Запустить тесты
    cmds:
      - npm run test -- --coverage
  
  build:
    desc: Собрать для окружения
    cmds:
      - echo "Сборка для {{.ENV | default \"development\"}}"
      - npm run build -- --mode={{.ENV}}
  
  # Композитные процессы
  ci:
    desc: Полный CI pipeline
    cmds:
      - task: clean
      - task: test
      - task: build
        vars:
          ENV: production
  
  deploy:
    desc: Деплой в окружение
    cmds:
      - task: build
        vars:
          ENV: "{{.TARGET_ENV}}"
      - ./scripts/deploy.sh {{.TARGET_ENV}} {{.VERSION}}
```

## Автоматическая синхронизация конфигурации

Razd автоматически синхронизирует Razdfile.yml и mise.toml, обеспечивая согласованность:

```bash
# Синхронизация происходит автоматически
razd up
razd install

# Отключить синхронизацию для конкретной команды
razd install --no-sync

# Или через переменную окружения
export RAZD_NO_SYNC=1
razd up
```

## Примеры использования

### Онбординг нового разработчика

```bash
# Новый член команды клонирует и настраивает проект
razd up https://github.com/company/api-service

# ✓ Репозиторий склонирован
# ✓ Node.js 20, PostgreSQL client, Redis tools установлены
# ✓ npm зависимости установлены
# ✓ База данных настроена и мигрирована
# ✓ Dev сервер запущен на localhost:3000
```

### CI/CD Pipeline

```bash
# В вашем .gitlab-ci.yml или .github/workflows
- razd up && razd run test && razd run build
```

### Работа с несколькими проектами

```bash
# Быстрое переключение между проектами
cd ~/projects/frontend
razd up  # Автоматически настроит Node.js окружение

cd ~/projects/backend
razd up  # Автоматически настроит Python окружение
```

### Мастер-классы и туториалы

```bash
# Участники мастер-класса могут сразу приступить к работе
razd up https://github.com/workshop/react-tutorial

# Все готово за 30 секунд
```

## Преимущества

### Для разработчиков
- ⏱️ **Экономия времени**: Настройка за 30 секунд вместо 10+ минут
- 🎯 **Фокус на коде**: Меньше времени на настройку окружения
- 🔄 **Единообразный процесс**: Одинаковая команда для всех проектов
- 📚 **Самодокументируемость**: Razdfile.yml — это живая документация

### Для команд
- 🤝 **Быстрый онбординг**: Новички продуктивны с первого дня
- ✅ **Согласованность**: Все используют одинаковые версии инструментов
- 🔧 **Меньше проблем "работает у меня"**: Воспроизводимые окружения
- 📦 **Стандартизация**: Единый подход для всех проектов

### Для проектов
- 🌍 **Кроссплатформенность**: Windows, macOS, Linux
- 📖 **Отличный DX**: Понятные сообщения об ошибках
- 🔌 **Интеграция с экосистемой**: Использует существующие инструменты
- 🚀 **Нулевая конфигурация**: Работает с существующими mise.toml и Taskfile.yml

## Сравнение с традиционным подходом

| Аспект | Традиционный подход | Razd |
|--------|---------------------|------|
| **Время настройки** | 5-15 минут | 30 секунд |
| **Количество команд** | 10+ команд | 1 команда |
| **Вероятность ошибок** | Высокая | Низкая |
| **Кривая обучения** | Крутая (для новичков) | Плавная |
| **Документация** | Часто устаревает | Самодокументируемая |
| **Кроссплатформенность** | Требует адаптации | Работает везде |

## Поддерживаемые экосистемы

Razd работает с любым проектом, который использует mise/asdf и taskfile, включая:

- **JavaScript/TypeScript**: Node.js, Deno, Bun, React, Vue, Angular, Next.js
- **Python**: Django, Flask, FastAPI, Poetry, pip
- **Ruby**: Rails, Sinatra, Bundler
- **Go**: любые Go проекты с go.mod
- **Rust**: Cargo проекты
- **PHP**: Composer проекты
- **Java/Kotlin**: Maven, Gradle
- **И более 500 других инструментов** через mise

## Следующие шаги

Теперь, когда вы понимаете, что такое Razd:

1. 📦 [Установите Razd](/installation/) в вашу систему
2. 🚀 Попробуйте [Руководство по началу работы](/getting-started)
3. ❓ Ознакомьтесь с [FAQ](/faq) для ответов на частые вопросы
4. 💻 Изучите [примеры проектов](https://github.com/razd-cli/razd/tree/main/examples)
5. 🐛 Сообщайте об ошибках на [GitHub Issues](https://github.com/razd-cli/razd/issues)

### 3. **task** - Автоматизация проекта
- Запускает специфичные для проекта задачи настройки
- Обрабатывает зависимости, базы данных, конфигурацию
- Предоставляет единый интерфейс для задач в разных проектах

## Ключевые преимущества

### 🚀 **Скорость**
- Одна команда заменяет 5-10 ручных шагов
- Параллельная установка инструментов где возможно
- Умное кэширование сокращает время повторной настройки

### 🌍 **Согласованность**
- Одинаковый процесс для всех проектов и платформ
- Стандартизированный процесс настройки для команд
- Работает идентично на Windows, macOS и Linux

### 🛡️ **Надёжность**
- Автоматическая обработка ошибок и восстановление
- Понятные сообщения об ошибках с предлагаемыми исправлениями
- Валидация каждого шага настройки

### 👥 **Удобство для команды**
- Новые члены команды становятся продуктивными быстрее
- Устраняет проблемы типа "у меня работает"
- Документированный процесс настройки через конфигурацию

### 🔧 **Интеграция**
- Работает с существующими настройками mise и taskfile
- Неинтрузивный - не изменяет структуру вашего проекта
- Расширяемый через пользовательские конфигурации

## Основные концепции

### Соглашения вместо конфигурации
Razd следует стандартным соглашениям:
- Ищет `.mise.toml` или `.tool-versions` для требований к инструментам
- Ожидает `Taskfile.yml` для задач проекта
- По умолчанию запускает задачу `setup`

### Нулевая конфигурация
Razd работает сразу с проектами, следующими стандартным паттернам:
- Не требуется специфичная для Razd конфигурация
- Использует существующие конфигурации mise и taskfile
- Корректно обрабатывает отсутствующие конфигурации

### Умное определение
Razd автоматически определяет и адаптируется к вашему проекту:
- Идентифицирует доступные конфигурационные файлы
- Корректно пропускает отсутствующие шаги
- Предоставляет обратную связь о происходящем

## Когда использовать Razd

### Идеально для:
- **Онбординга команды**: Делает новых разработчиков продуктивными немедленно
- **Личной продуктивности**: Устраняет повторяющиеся задачи настройки
- **CI/CD пайплайнов**: Согласованная настройка окружения в автоматизации
- **Шаблонов проектов**: Создание переиспользуемых настроек проектов
- **Мастер-классов/Туториалов**: Быстрая настройка проекта для участников

### Отлично работает с:
- Современными стеками разработки (Node.js, Python, Rust, Go и др.)
- Монорепозиториями с единообразным инструментарием
- Проектами с автоматизированными процедурами настройки
- Командами, использующими mise для управления инструментами
- Проектами с автоматизацией Taskfile.yml

## Что делает Razd особенным

В отличие от других инструментов настройки, Razd:

### Интегрирует существующие инструменты
- Не изобретает велосипед
- Использует проверенные инструменты (git, mise, task)
- Работает с вашими существующими процессами

### Фокусируется на опыте разработчика
- Разработан для ежедневного использования разработчиками
- Минимальная кривая обучения
- Понятные, действенные сообщения об ошибках

### Уважает автономию проекта
- Проекты контролируют свой собственный процесс настройки
- Razd просто оркестрирует существующие конфигурации
- Нет vendor lock-in или проприетарных форматов

### Кроссплатформенный по природе
- Построен на Rust для производительности и надёжности
- Единый исполняемый файл без зависимостей времени выполнения
- Согласованное поведение на всех платформах

## Начало работы

Готовы попробовать Razd? Вот что делать дальше:

1. **[Установить Razd](/installation/)** - Руководство по быстрой установке
2. **[Быстрый старт](/getting-started)** - Ваша первая настройка проекта
3. **[Примеры](/faq#usage-questions)** - Посмотрите реальные паттерны использования

## Философия

Razd построен на принципе, что **настройка проекта должна быть простой**. Мы верим:

- Разработчики должны фокусироваться на создании, а не на настройке
- Процессы настройки должны быть быстрыми, надёжными и согласованными
- Инструменты должны бесшовно интегрироваться с существующими процессами
- Автоматизация должна улучшать, а не заменять знания разработчика

Razd воплощает русскую концепцию "раздуплиться" - проснуться, организоваться и начать эффективно двигаться. Это о преодолении трения при настройке, чтобы вы могли заняться тем, что важно: созданием отличного программного обеспечения.